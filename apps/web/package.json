{
  "name": "@myhandymanapp/web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "cf:build": "tsc --noEmit && vite build",
    "cf:preview": "tsc --noEmit && vite build && npx wrangler@latest dev",
    "cf:deploy": "npx wrangler@latest deploy"
  },
  "dependencies": {
    "@react-oauth/google": "0.12.1",
    "lucide-react": "^0.541.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "19.0.8",
    "@types/react-dom": "19.0.3",
    "@vitejs/plugin-react": "4.3.1",
    "typescript": "^5.9.3",
    "vite": "4.5.9"
  }
}
