[build]
  # ✅ Skip build unless web-next or shared packages changed
  ignore = "git diff --name-only \"$CACHED_COMMIT_REF\" \"$COMMIT_REF\" | grep -E '^(apps/web-next/|packages/shared/)' >/dev/null && exit 1 || exit 0"
